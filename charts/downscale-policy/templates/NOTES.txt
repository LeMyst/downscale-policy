downscale-policy {{ .Chart.AppVersion }} deployed in namespace {{ .Release.Namespace }}.

Check that the operator is running:

  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

Namespace users can now create a DownscalePolicy in their namespace, e.g.:

  apiVersion: downscaler.io/v1
  kind: DownscalePolicy
  metadata:
    name: office-hours
  spec:
    uptime: "Mon-Fri 07:30-19:30 Europe/Paris"
    downtimeReplicas: 0

and inspect it with:

  kubectl get dsp

{{- if not (and .Values.rbac.create .Values.rbac.userRoles) }}

NOTE: the user-facing editor/viewer ClusterRoles were not created
(rbac.create/rbac.userRoles is disabled). Grant users access to
DownscalePolicy resources yourself, otherwise nobody but cluster admins
can create them.
{{- else if not .Values.rbac.aggregateToDefaultRoles }}

NOTE: rbac.aggregateToDefaultRoles is disabled. Bind
{{ include "downscale-policy.fullname" . }}-editor / -viewer to your users
explicitly, otherwise nobody but cluster admins can create DownscalePolicies.
{{- end }}
{{- if not .Values.crds.install }}

NOTE: crds.install is disabled. The DownscalePolicy CRD must be installed
separately before the operator can work:

  kubectl apply -f https://github.com/LeMyst/downscale-policy/raw/main/charts/downscale-policy/files/crd/downscaler.io_downscalepolicies.yaml
{{- end }}
